# Trade locale snapshots

Generated with `pnpm data:refresh` from the official Path of Exile Trade APIs.

Commit the generated JSON files with a release when the official data changes.
`zh-cn` uses the `zh-tw` official snapshot and is converted locally by the extension.
